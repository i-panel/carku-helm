apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "carku.fullname" . }}-test-connection"
  labels:
    {{- include "carku.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "carku.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
